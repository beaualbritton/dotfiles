monitor=,preferred,auto,1

$mod = SUPER

exec-once = waybar

bind = $mod, RETURN, exec, ghostty
bind = $mod, Q, killactive
bind = $mod, M, exit

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
}

decoration {
    rounding = 10
}

input {
    kb_layout = us
}

